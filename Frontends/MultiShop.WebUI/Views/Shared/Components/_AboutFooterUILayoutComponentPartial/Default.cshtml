@model List<ResultAboutDto>

@foreach (var item in Model)
{
    <h5 class="text-secondary text-uppercase mb-4">Hakkımızda</h5>
    <p class="mb-4">@item.Description</p>
    <p class="mb-2"><i class="fa fa-map-marker-alt text-primary mr-3"></i>@item.Address</p>
    <p class="mb-2"><i class="fa fa-envelope text-primary mr-3"></i>@item.Mail</p>
    <p class="mb-0"><i class="fa fa-phone-alt text-primary mr-3"></i>@item.Telefon</p>
}