@model OrderViewModel

<h5 class="section-title position-relative text-uppercase mb-3"><span class="bg-secondary pr-3">Kargo Şirketi</span></h5>
<div class="bg-light p-30 mb-5">
    <div class="pt-2">
        <div class="d-flex justify-content-between mt-2">
            <select id="CargoCompanyList" asp-for="SelectedCargoCompany" asp-items="@ViewBag.CargoCompanies" class="form-control"></select>
        </div>
    </div>
</div>